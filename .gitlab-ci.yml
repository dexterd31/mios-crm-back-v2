stages:
    -  quality
deploy:
    only:
        - Quality
        - merge request
        
    stage: quality
    script:
        - cd /var/www/mios/mios-backend/mios-crm-back-v2
        - git pull
    tags:
        - crm-back-quality
